import React from "react";
import Base from "../core/Base";

const Profile = () => {
  return (
    <Base title="Profile Page" description="This is the profile of user">
      <h1>This is profile page</h1>
    </Base>
  );
};

export default Profile;
